my.display<-function(x, display=FALSE, type=NA, prob){
    if (display) 
        parse(text=paste(type,"()") else 
        if (display & type==NA) 
            return("Please specify type as either hist or density")
}
